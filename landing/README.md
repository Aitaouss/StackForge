# StackForge landing page

Marketing site for the `create-stackforge-app` npm package.

## Run locally

```bash
cd landing
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
pnpm build
pnpm start
```
