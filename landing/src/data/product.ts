/** Published create-stackforge-app version shown on the landing site. */
export const CLI_VERSION = "1.4.1";

/** Hero / changelog link (GitHub release). */
/** Prefer /latest until the v{CLI_VERSION} GitHub release is published. */
export const RELEASE_NOTES_URL = "https://github.com/Aitaouss/StackForge/releases/latest";

export const PRIMARY_INSTALL_CMD = "npx create-stackforge-app@latest";

export const STACKFORGE_TAGLINE =
  "apps/web + apps/api monorepo — presets, manifest v2, stackforge doctor, auth, CRUD, tests, Docker, and CI.";
