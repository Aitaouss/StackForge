# <%= projectName %> — agent guide Generated by **create-stackforge-app** v<%=
stackforgeVersion %>. ## Architecture | App | Path | |-----|------| | Frontend
(Next.js) | `frontend/` | | Backend (NestJS) | `backend/` | Project metadata:
`stackforge.json` (schemaVersion 1). ## File ownership (StackForge) | Category |
Policy | |----------|--------| | **Managed** | StackForge may update on future
`stackforge upgrade` (e.g. `.stackforge/`, shared lint/tsconfig bases, CI
workflow when added). | | **Extended** | StackForge owns structure; you add
logic (e.g. `backend/src/auth/`). | | **User** | Your domain code — StackForge
never overwrites (e.g. custom modules, features from `stackforge g resource` in
future releases). | ## Rules - Controllers stay thin; business logic lives in
services. - Do not store plaintext passwords; use existing auth patterns. -
Protected API routes require JWT (`AuthGuard`). - Frontend API calls go through
`frontend/src/services/` (Axios + React Query hooks). ## Commands ```bash pnpm
install pnpm dev pnpm build ``` See root `README.md` for database and Docker
setup.
