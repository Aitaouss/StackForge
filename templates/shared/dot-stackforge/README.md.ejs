# StackForge managed metadata

This directory is reserved for StackForge tooling (manifest locks, upgrade hints).

Do not delete `stackforge.json` in the project root — future CLI commands use it to understand this project.
